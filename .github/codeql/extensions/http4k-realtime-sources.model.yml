extensions:
  - addsTo:
      pack: codeql/java-all
      extensible: sourceModel
    data:
      # --- WsMessage (data class in org.http4k.websocket, file: WsMessage.kt) ---
      # bodyString() - WebSocket message body as string
      - ["org.http4k.websocket", "WsMessage", false, "bodyString", "()", "", "remote", "ReturnValue", "manual"]
      # body property - WebSocket message body
      - ["org.http4k.websocket", "WsMessage", false, "getBody", "()", "", "remote", "ReturnValue", "manual"]

      # --- SseMessage.Data (sealed interface inner data class in org.http4k.sse) ---
      # data property - SSE event data
      - ["org.http4k.sse", "SseMessage$Data", false, "getData", "()", "", "remote", "ReturnValue", "manual"]

      # --- SseMessage.Event (sealed interface inner data class in org.http4k.sse) ---
      # data property - SSE event data payload
      - ["org.http4k.sse", "SseMessage$Event", false, "getData", "()", "", "remote", "ReturnValue", "manual"]
      # event property - SSE event type name
      - ["org.http4k.sse", "SseMessage$Event", false, "getEvent", "()", "", "remote", "ReturnValue", "manual"]
      # id property - SSE event ID
      - ["org.http4k.sse", "SseMessage$Event", false, "getId", "()", "", "remote", "ReturnValue", "manual"]
